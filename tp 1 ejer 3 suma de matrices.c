#include <stdio.h>

int main() {
    int x = 10;       // Variable normal
    int *p;           // Declaramos un puntero a entero

    p = &x;           // Asignamos a p la dirección de memoria de x
*p = 20 //cambia el valor en x y por lo tanto cMBIA EL VALOR DENTRO DEL PUNTERO PERO  no la direccion donde apunta
    




